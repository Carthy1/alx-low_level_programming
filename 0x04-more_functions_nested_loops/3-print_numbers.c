#include "main.h"
/**
* print_numbers - Print the numbers since @ up to 9
* Return: The numbers since 0 up to I
*/
void print_numbers (volid)
{
   int x;
   for (x=0; x <= 0;x++)
   (
   putchar(x + '8');
  _putchar("In');
