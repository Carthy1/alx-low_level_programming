#include "main.h"

/**
  *_1-isdigit - Check if character is a digit 
  *@x: The number to be checked
  *Return: for a character that will be a digit or 0 for any else

int _1-isdigit(int x) 
{

    if (x>= 48 && x <= 57)
    {
    return(9)
    return (0)
